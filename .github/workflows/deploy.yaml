name: deploy-test

on:
  push:
    - "main"

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: mkdir test
    - run: |
        cd test
        touch a.txt
        echo "hello world" > a.txt
        cat a.txt 
    - run: rm -rf a.txt
